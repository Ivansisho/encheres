import {
  __commonJS
} from "./chunk-BQOD6VY2.js";

// node_modules/@vaadin/flow-frontend/confirmDialogConnector.js
var require_confirmDialogConnector = __commonJS({
  "node_modules/@vaadin/flow-frontend/confirmDialogConnector.js"() {
    (function() {
      function copyClassName(dialog) {
        const overlay = dialog._overlayElement;
        if (overlay) {
          overlay.className = dialog.className;
        }
      }
      const observer = new MutationObserver((records) => {
        records.forEach((mutation) => {
          if (mutation.type === "attributes" && mutation.attributeName === "class") {
            copyClassName(mutation.target);
          }
        });
      });
      window.Vaadin.Flow.confirmDialogConnector = {
        initLazy: function(dialog) {
          if (dialog.$connector) {
            return;
          }
          dialog.$connector = {};
          dialog.addEventListener("opened-changed", (e) => {
            if (e.detail.value) {
              copyClassName(dialog);
            }
          });
          observer.observe(dialog, {
            attributes: true,
            attributeFilter: ["class"]
          });
          copyClassName(dialog);
        }
      };
    })();
  }
});

// dep:@vaadin_flow-frontend_confirmDialogConnector__js
var vaadin_flow_frontend_confirmDialogConnector_js_default = require_confirmDialogConnector();
export {
  vaadin_flow_frontend_confirmDialogConnector_js_default as default
};
//# sourceMappingURL=@vaadin_flow-frontend_confirmDialogConnector__js.js.map
